## 1. Application 层筛选能力

- [x] 1.1 在 `src/types/index.ts` 中新增奖励列表筛选类型，覆盖全部、可领取、已领取、已过期四种视图。
- [x] 1.2 扩展 `src/application/services/RewardService.ts`，新增基于 `Reward` 状态方法的列表筛选能力。
- [x] 1.3 为 service 层筛选行为补充或更新单元测试，覆盖默认场景和空结果场景。

## 2. 奖励页面交互

- [x] 2.1 更新 `src/ui/views/RewardPage.vue`，维护当前筛选状态并渲染全部、可领取、已领取、已过期四类筛选控件。
- [x] 2.2 基于当前筛选结果渲染可见奖励列表，并让数量统计和空状态文案与当前筛选条件保持一致。
- [x] 2.3 保持筛选状态下的领取流程不变，确保领取成功后页面按当前筛选条件稳定刷新。

## 3. 验证

- [x] 3.1 补充或更新组件测试，覆盖筛选切换、筛选后的列表渲染和空状态展示。
- [x] 3.2 运行 `npm run typecheck` 和 `npm run test`，验证奖励筛选变更整体可用。
